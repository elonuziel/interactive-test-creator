[pytest]
testpaths = quiz-builder-app/tests_py
pythonpath = quiz-builder-app/src
norecursedirs = cli-legacy node_modules build dist .git scratch
addopts = -ra --ignore=cli-legacy --ignore-glob=cli-legacy/*

